diff --git a/src/raop_server.c b/src/raop_server.c
index 53a89ff..3d97ef1 100644
--- a/src/raop_server.c
+++ b/src/raop_server.c
@@ -63,6 +63,10 @@ typedef struct raopsr_s {
 		uint16_t base, range;
 	} ports;
 	int http_length;
+	bool soft_flush;
+	uint32_t prebuffer_ms;
+	pthread_mutex_t session_mutex;
+	uint32_t last_rtsp_command_ms, rtsp_control_closed_ms, control_disconnects;
 } raopsr_t;
 
 extern log_level	raop_loglevel;
@@ -106,6 +110,7 @@ struct raopsr_s *raopsr_create(struct in_addr host, struct mdnsd *svr, char *nam
 
 	// make sure we have a clean context
 	memset(ctx, 0, sizeof(raopsr_t));
+	pthread_mutex_init(&ctx->session_mutex, NULL);
 
 	ctx->http_length = http_length;
 	ctx->ports.base = port_base;
@@ -133,6 +138,7 @@ struct raopsr_s *raopsr_create(struct in_addr host, struct mdnsd *svr, char *nam
 	if (ctx->sock < 0 || listen(ctx->sock, 4)) {
 		LOG_ERROR("Cannot bind or listen RTSP listener: %s", strerror(errno));
 		closesocket(ctx->sock);
+		pthread_mutex_destroy(&ctx->session_mutex);
 		free(ctx);
 		return NULL;
 	}
@@ -189,6 +195,32 @@ void raopsr_update(struct raopsr_s *ctx, char *name, char *model) {
 	free(id);
 }
 
+void raopsr_set_stream_options(struct raopsr_s *ctx, bool soft_flush, uint32_t prebuffer_ms) {
+	if (!ctx) return;
+	ctx->soft_flush = soft_flush;
+	ctx->prebuffer_ms = prebuffer_ms;
+}
+
+bool raopsr_get_stats(struct raopsr_s *ctx, raopsr_stats_t *stats) {
+	if (!ctx || !stats) return false;
+	memset(stats, 0, sizeof(*stats));
+	pthread_mutex_lock(&ctx->session_mutex);
+	stats->last_rtsp_command_ms = ctx->last_rtsp_command_ms;
+	stats->rtsp_control_closed_ms = ctx->rtsp_control_closed_ms;
+	stats->control_disconnects = ctx->control_disconnects;
+	bool ok = ctx->ht && raopst_get_stats(ctx->ht, stats);
+	pthread_mutex_unlock(&ctx->session_mutex);
+	return ok;
+}
+
+bool raopsr_discard_to_live_edge(struct raopsr_s *ctx) {
+	if (!ctx) return false;
+	pthread_mutex_lock(&ctx->session_mutex);
+	bool ok = ctx->ht && raopst_discard_to_live_edge(ctx->ht);
+	pthread_mutex_unlock(&ctx->session_mutex);
+	return ok;
+}
+
 /*----------------------------------------------------------------------------*/
 void raopsr_delete(struct raopsr_s *ctx) {
 	if (!ctx) return;
@@ -197,7 +229,12 @@ void raopsr_delete(struct raopsr_s *ctx) {
 	pthread_join(ctx->thread, NULL);
 
 	raopsr_metadata_free(&ctx->metadata);
-	raopst_end(ctx->ht);
+	pthread_mutex_lock(&ctx->session_mutex);
+	struct raopst_s *ht = ctx->ht;
+	ctx->ht = NULL;
+	pthread_mutex_unlock(&ctx->session_mutex);
+	raopst_end(ht);
+	pthread_mutex_destroy(&ctx->session_mutex);
 
 #if WIN
 	shutdown(ctx->sock, SD_BOTH);
@@ -373,6 +410,10 @@ static void *rtsp_thread(void *arg) {
 		n = select(nfds, &rfds, NULL, NULL, &timeout);
 		
 		if (n < 0) {
+			pthread_mutex_lock(&ctx->session_mutex);
+			ctx->rtsp_control_closed_ms = gettime_ms();
+			ctx->control_disconnects++;
+			pthread_mutex_unlock(&ctx->session_mutex);
 			closesocket(sock);
 			LOG_INFO("RTSP close %u", sock);
 			sock = -1;
@@ -383,6 +424,10 @@ static void *rtsp_thread(void *arg) {
 
 		if (FD_ISSET(ctx->sock, &rfds) && gettime_ms() - last_rx > RTSP_IDLE_TIMEOUT_MS) {
 			LOG_WARN("RTSP %u idle for %us with a connection pending, displacing it", sock, (unsigned)((gettime_ms() - last_rx) / 1000));
+			pthread_mutex_lock(&ctx->session_mutex);
+			ctx->rtsp_control_closed_ms = gettime_ms();
+			ctx->control_disconnects++;
+			pthread_mutex_unlock(&ctx->session_mutex);
 			closesocket(sock);
 			sock = -1;
 			continue;
@@ -394,6 +439,10 @@ static void *rtsp_thread(void *arg) {
 		res = handle_rtsp(ctx, sock);
 
 		if (!res) {
+			pthread_mutex_lock(&ctx->session_mutex);
+			ctx->rtsp_control_closed_ms = gettime_ms();
+			ctx->control_disconnects++;
+			pthread_mutex_unlock(&ctx->session_mutex);
 			closesocket(sock);
 			LOG_INFO("RTSP close %u", sock);
 			sock = -1;
@@ -420,6 +469,10 @@ static bool handle_rtsp(raopsr_t *ctx, int sock)
 		return false;
 	}
 
+	pthread_mutex_lock(&ctx->session_mutex);
+	ctx->last_rtsp_command_ms = gettime_ms();
+	pthread_mutex_unlock(&ctx->session_mutex);
+
 	if (strcmp(method, "OPTIONS")) {
 		LOG_INFO("[%p]: received %s", ctx, method);
 	}
@@ -511,7 +564,10 @@ static bool handle_rtsp(raopsr_t *ctx, int sock)
 							ctx->ports.range, ctx->http_length);
 
 		ctx->hport = ht.hport;
+		pthread_mutex_lock(&ctx->session_mutex);
 		ctx->ht = ht.ctx;
+		raopst_set_options(ctx->ht, ctx->soft_flush, ctx->prebuffer_ms);
+		pthread_mutex_unlock(&ctx->session_mutex);
 		ctx->flushedArtwork = true;
 
 		if ((cport * tport * ht.cport * ht.tport * ht.aport * ht.hport) != 0 && ht.ctx) {
@@ -567,9 +623,11 @@ static bool handle_rtsp(raopsr_t *ctx, int sock)
 
 		ctx->raop_cb(ctx->owner, RAOP_STOP);
 		raopsr_metadata_free(&ctx->metadata);
-		raopst_end(ctx->ht);
-
+		pthread_mutex_lock(&ctx->session_mutex);
+		struct raopst_s *ht = ctx->ht;
 		ctx->ht = NULL;
+		pthread_mutex_unlock(&ctx->session_mutex);
+		raopst_end(ht);
 		ctx->hport = -1;
 
 		// need to make sure no search is on-going and reclaim pthread memory
diff --git a/src/raop_server.h b/src/raop_server.h
index 642fc9f..944154d 100644
--- a/src/raop_server.h
+++ b/src/raop_server.h
@@ -23,6 +23,17 @@ typedef enum { RAOP_STREAM, RAOP_PLAY, RAOP_FLUSH, RAOP_PAUSE, RAOP_STOP, RAOP_V
 typedef void (*raopsr_cb_t)(void *owner, raopsr_event_t event, ...);
 typedef void (*raop_http_cb_t)(void *owner, struct key_data_s *headers, struct key_data_s *response);
 
+typedef struct {
+	uint32_t written, read, fill;
+	uint32_t last_rtsp_command_ms, rtsp_control_closed_ms;
+	uint32_t last_rtp_ms, last_sync_ms, last_audio_ms, last_decoded_ms;
+	uint32_t last_write_ms, last_read_ms, first_rtp_ms, first_sync_ms, first_audio_ms;
+	uint32_t http_open_ms, http_close_ms, source_rtp_timestamp, last_contiguous_rtp_timestamp;
+	uint32_t control_disconnects, http_disconnects, http_reconnects, discontinuities;
+	uint64_t http_bytes_served, source_pcm_frames, output_pcm_frames;
+	bool http_connected;
+} raopsr_stats_t;
+
 // set http_length to -3 for chunked-encoding, 0 for no content-length or to a positive value
 struct raopsr_s* raopsr_create(struct in_addr host, struct mdnsd *svr, char *name,
 						  char *model, unsigned char mac[6], char *stream_codec, bool stream_metadata,
@@ -33,6 +44,9 @@ struct raopsr_s* raopsr_create(struct in_addr host, struct mdnsd *svr, char *nam
 void	raopsr_update(struct raopsr_s *ctx, char *name, char *model);
 void  	raopsr_delete(struct raopsr_s *ctx);
 void	raopsr_notify(struct raopsr_s *ctx, raopsr_event_t event, void *param);
+void	raopsr_set_stream_options(struct raopsr_s *ctx, bool soft_flush, uint32_t prebuffer_ms);
+bool	raopsr_get_stats(struct raopsr_s *ctx, raopsr_stats_t *stats);
+bool	raopsr_discard_to_live_edge(struct raopsr_s *ctx);
 
 void	raopsr_metadata_free(raopsr_metadata_t* data);
 void	raopsr_metadata_copy(raopsr_metadata_t* dst, raopsr_metadata_t *src);
diff --git a/src/raop_streamer.c b/src/raop_streamer.c
index d09ffed..a6f9ac6 100644
--- a/src/raop_streamer.c
+++ b/src/raop_streamer.c
@@ -123,6 +123,11 @@ typedef struct raopst_s {
 	size_t http_count;
 	int http_length;
 	bool close_socket;
+	bool soft_flush, start_notified;
+	uint32_t prebuffer_ms, last_write_ms, last_read_ms, first_rtp_ms, first_sync_ms, first_audio_ms;
+	uint32_t last_rtp_ms, last_sync_ms, last_audio_ms, last_decoded_ms, http_open_ms, http_close_ms;
+	uint32_t source_rtp_timestamp, last_contiguous_rtp_timestamp, http_disconnects, http_reconnects, discontinuities;
+	uint64_t http_bytes_served, source_pcm_frames, output_pcm_frames;
 } raopst_t;
 
 #define BUFIDX(seqno) ((seq_t)(seqno) % BUFFER_FRAMES)
@@ -301,6 +306,59 @@ void raopst_metadata(struct raopst_s *ctx, raopsr_metadata_t *metadata) {
 	pthread_mutex_unlock(&ctx->ab_mutex);
 }
 
+void raopst_set_options(struct raopst_s *ctx, bool soft_flush, uint32_t prebuffer_ms) {
+	if (!ctx) return;
+	pthread_mutex_lock(&ctx->ab_mutex);
+	ctx->soft_flush = soft_flush;
+	ctx->prebuffer_ms = prebuffer_ms;
+	pthread_mutex_unlock(&ctx->ab_mutex);
+}
+
+bool raopst_get_stats(struct raopst_s *ctx, raopsr_stats_t *stats) {
+	if (!ctx || !stats) return false;
+	pthread_mutex_lock(&ctx->ab_mutex);
+	stats->written = ctx->ab_write;
+	stats->read = ctx->ab_read;
+	stats->fill = (uint16_t) (ctx->ab_write - ctx->ab_read + 1);
+	stats->last_write_ms = ctx->last_write_ms;
+	stats->last_read_ms = ctx->last_read_ms;
+	stats->first_rtp_ms = ctx->first_rtp_ms;
+	stats->first_sync_ms = ctx->first_sync_ms;
+	stats->first_audio_ms = ctx->first_audio_ms;
+	stats->last_rtp_ms = ctx->last_rtp_ms;
+	stats->last_sync_ms = ctx->last_sync_ms;
+	stats->last_audio_ms = ctx->last_audio_ms;
+	stats->last_decoded_ms = ctx->last_decoded_ms;
+	stats->http_open_ms = ctx->http_open_ms;
+	stats->http_close_ms = ctx->http_close_ms;
+	stats->source_rtp_timestamp = ctx->source_rtp_timestamp;
+	stats->last_contiguous_rtp_timestamp = ctx->last_contiguous_rtp_timestamp;
+	stats->http_disconnects = ctx->http_disconnects;
+	stats->http_reconnects = ctx->http_reconnects;
+	stats->discontinuities = ctx->discontinuities;
+	stats->http_bytes_served = ctx->http_bytes_served;
+	stats->source_pcm_frames = ctx->source_pcm_frames;
+	stats->output_pcm_frames = ctx->output_pcm_frames;
+	stats->http_connected = ctx->http_ready;
+	pthread_mutex_unlock(&ctx->ab_mutex);
+	return true;
+}
+
+bool raopst_discard_to_live_edge(struct raopst_s *ctx) {
+	if (!ctx) return false;
+	pthread_mutex_lock(&ctx->ab_mutex);
+	buffer_reset(ctx->audio_buffer);
+	ctx->ab_read = ctx->ab_write + 1;
+	ctx->http_count = ctx->silence_count = 0;
+	ctx->close_socket = true;
+	ctx->http_ready = false;
+	ctx->discontinuities++;
+	encoder_close(ctx->encoder);
+	encoder_open(ctx->encoder);
+	pthread_mutex_unlock(&ctx->ab_mutex);
+	return true;
+}
+
 /*---------------------------------------------------------------------------*/
 void raopst_end(raopst_t *ctx) {
 	if (!ctx) return;
@@ -338,7 +396,15 @@ bool raopst_flush(raopst_t *ctx, unsigned short seqno, unsigned int rtptime, boo
 	ctx->first_seqno = seqno;
 	bool flushed = true;
 
-	if (silence) {
+	if (ctx->soft_flush) {
+		buffer_reset(ctx->audio_buffer);
+		ctx->state = RTP_WAIT;
+		ctx->synchro.first = false;
+		ctx->ab_read = ctx->ab_write + 1;
+		ctx->http_count = 0;
+		ctx->start_notified = false;
+		encoder_close(ctx->encoder);
+	} else if (silence) {
 		ctx->pause = true;
 	} else if (ctx->state == RTP_PLAY) {
 		buffer_reset(ctx->audio_buffer);
@@ -416,6 +482,7 @@ static void alac_decode(raopst_t *ctx, int16_t *dest, char *buf, int len, int *o
 /*---------------------------------------------------------------------------*/
 static void buffer_put_packet(raopst_t* ctx, seq_t seqno, unsigned rtptime, bool first, char* data, int len) {
 	pthread_mutex_lock(&ctx->ab_mutex);
+	ctx->last_rtp_ms = ctx->last_audio_ms = gettime_ms();
 
 	/* if we have received a RECORD with a seqno, then this is the first allowed rtp sequence number 
 	 * and we are in RTP_WAIT state. If seqno was 0, then we are waiting for a flush that will tell 
@@ -495,6 +562,8 @@ static void buffer_put_packet(raopst_t* ctx, seq_t seqno, unsigned rtptime, bool
 	if (seqno == (uint16_t) (ctx->ab_write + 1)) {
 		// expected packet
 		ctx->ab_write = seqno;
+		ctx->last_write_ms = gettime_ms();
+		ctx->last_contiguous_rtp_timestamp = rtptime;
 		LOG_SDEBUG("[%p]: packet expected seqno:%hu rtptime:%u (W:%hu R:%hu)", ctx, seqno, rtptime, ctx->ab_write, ctx->ab_read);
 	} else if (seq_order(ctx->ab_write, seqno)) {
 		// newer than expected
@@ -519,6 +588,7 @@ static void buffer_put_packet(raopst_t* ctx, seq_t seqno, unsigned rtptime, bool
 		}
 		LOG_DEBUG("[%p]: packet newer seqno:%hu rtptime:%u (W:%hu R:%hu)", ctx, seqno, rtptime, ctx->ab_write, ctx->ab_read);
 		ctx->ab_write = seqno;
+		ctx->last_write_ms = gettime_ms();
 	} else if (seq_order(ctx->ab_read, seqno + 1)) {
 		// recovered packet, not yet sent
 		LOG_DEBUG("[%p]: packet recovered seqno:%hu rtptime:%u (W:%hu R:%hu)", ctx, seqno, rtptime, ctx->ab_write, ctx->ab_read);
@@ -533,6 +603,7 @@ static void buffer_put_packet(raopst_t* ctx, seq_t seqno, unsigned rtptime, bool
 
 	if (abuf) {
 		alac_decode(ctx, abuf->data, data, len, &abuf->len);
+		ctx->last_decoded_ms = gettime_ms();
 		abuf->ready = true;
 		abuf->missed = false;
 		// this is the local rtptime when this frame is expected to play
@@ -547,8 +618,13 @@ static void buffer_put_packet(raopst_t* ctx, seq_t seqno, unsigned rtptime, bool
 		if (silence && ctx->ab_write - ctx->ab_read > 1) ctx->audio_buffer[BUFIDX(ctx->ab_read++)].ready = false;
 
 		if (ctx->state == RTP_PLAY && ctx->silence && !silence) {
-			ctx->event_cb(ctx->owner, RAOP_STREAMER_PLAY);
-			ctx->silence = false;
+			uint32_t frames = (uint16_t) (ctx->ab_write - ctx->ab_read + 1);
+			uint32_t needed = (ctx->prebuffer_ms * 44100) / (ctx->frame_size * 1000);
+			if (!ctx->start_notified && frames >= needed) {
+				ctx->event_cb(ctx->owner, RAOP_STREAMER_PLAY);
+				ctx->start_notified = true;
+				ctx->silence = false;
+			}
 			// if we have some metadata, just do a refresh (case of FLUSH not sending metadata)
 			if (ctx->metadata.title) ctx->icy.updated = true;
 		}
@@ -625,9 +701,9 @@ static void *rtp_thread_func(void *arg) {
 				if (plen < 16) break;
 
 				if ((packet[1] & 0x80) && (type != 0x56)) {
+					if (!ctx->first_audio_ms) ctx->first_audio_ms = gettime_ms();
 					LOG_INFO("[%p]: 1st audio packet received %hu", ctx, seqno);
 				}
-
 				buffer_put_packet(ctx, seqno, rtptime, packet[1] & 0x80, pktp, plen);
 				break;
 			}
@@ -638,6 +714,7 @@ static void *rtp_thread_func(void *arg) {
 				uint32_t rtp_now = ntohl(*(uint32_t*)(pktp+16));
 
 				pthread_mutex_lock(&ctx->ab_mutex);
+				ctx->last_sync_ms = gettime_ms();
 
 				// memorize that remote timing for when NTP adjustment arrives
 				ctx->timing.rtp_remote = (((uint64_t)ntohl(*(uint32_t*)(pktp + 8))) << 32) + ntohl(*(uint32_t*)(pktp + 12));
@@ -648,12 +725,14 @@ static void *rtp_thread_func(void *arg) {
 
 				// now we are synced on RTP frames
 				if ((ctx->synchro.status & RTP_SYNC) == 0) {
+					ctx->first_rtp_ms = gettime_ms();
 					ctx->synchro.status |= RTP_SYNC;
 					LOG_INFO("[%p]: 1st RTP packet received", ctx);
 				}
 
 				// 1st sync packet received (signals a restart of playback)
 				if (packet[0] & 0x10) {
+					if (!ctx->first_sync_ms) ctx->first_sync_ms = gettime_ms();
 					ctx->synchro.first = true;
 					LOG_INFO("[%p]: 1st sync packet received", ctx);
 				}
@@ -900,6 +979,8 @@ static short *_buffer_get_frame(raopst_t *ctx, size_t *bytes) {
 		*bytes = ctx->frame_size * 4;
 	} else {
 		*bytes = curframe->len;
+		ctx->source_rtp_timestamp = curframe->rtptime;
+		ctx->source_pcm_frames += *bytes / 4;
 		curframe->ready = 0;
 	}
 
@@ -912,6 +993,7 @@ static short *_buffer_get_frame(raopst_t *ctx, size_t *bytes) {
 	}
 
 	ctx->ab_read++;
+	ctx->last_read_ms = gettime_ms();
 	return curframe->data;
 }
 
@@ -962,12 +1044,18 @@ static void *http_thread_func(void *arg) {
 
 		int n = select(sock + 1, &rfds, NULL, NULL, &timeout);
 		bool res = true;
+		bool was_http_ready;
 
 		pthread_mutex_lock(&ctx->ab_mutex);
+		was_http_ready = ctx->http_ready;
 
 		if (n > 0) {
 			res = handle_http(ctx, sock);
 			ctx->http_ready = res;
+			if (res && !was_http_ready) {
+				ctx->http_open_ms = gettime_ms();
+				if (ctx->http_close_ms) ctx->http_reconnects++;
+			}
 
 			// only send silence when it's the first GET (or after a flush)
 			if (!ctx->http_count) {
@@ -982,6 +1070,10 @@ static void *http_thread_func(void *arg) {
 
 		// terminate connection if required by HTTP peer
 		if (n < 0 || !res || ctx->close_socket) {
+			if (was_http_ready || ctx->http_ready) {
+				ctx->http_close_ms = gettime_ms();
+				ctx->http_disconnects++;
+			}
 			LOG_INFO("HTTP close %u", sock);
 			closesocket(sock);
 			sock = -1;
@@ -994,6 +1086,7 @@ static void *http_thread_func(void *arg) {
 		// wait for session to be ready before sending (no need for mutex)
 		if (ctx->http_ready && (pcm = _buffer_get_frame(ctx, &bytes)) != NULL) {
 			size_t frames = bytes / 4;
+			ctx->output_pcm_frames += frames;
 			uint8_t* data = encoder_encode(ctx->encoder, pcm, frames, &bytes);
 
 			if (bytes) {
@@ -1052,6 +1145,7 @@ static void *http_thread_func(void *arg) {
 
 				LOG_SDEBUG("[%p]: HTTP sent frame count:%u bytes:%u (W:%hu R:%hu)", ctx, frame_count++, bytes + offset, ctx->ab_write, ctx->ab_read);
 				ssize_t sent = send_data(ctx->http_length == -3, sock, data + offset , bytes, 0);
+				if (sent > 0) ctx->http_bytes_served += sent;
 
 				// update remaining count with desired length
 				if (ctx->icy.active) ctx->icy.remain -= bytes;
diff --git a/src/raop_streamer.h b/src/raop_streamer.h
index da75199..cbb087c 100644
--- a/src/raop_streamer.h
+++ b/src/raop_streamer.h
@@ -33,3 +33,6 @@ bool 				raopst_flush(struct raopst_s *ctx, unsigned short seqno, unsigned rtpti
 void 				raopst_flush_release(struct raopst_s *ctx);
 void 				raopst_record(struct raopst_s *ctx, unsigned short seqno, unsigned rtptime);
 void 				raopst_metadata(struct raopst_s *ctx, raopsr_metadata_t *metadata);
+void				raopst_set_options(struct raopst_s *ctx, bool soft_flush, uint32_t prebuffer_ms);
+bool				raopst_get_stats(struct raopst_s *ctx, raopsr_stats_t *stats);
+bool				raopst_discard_to_live_edge(struct raopst_s *ctx);
diff --git a/targets/include/raop_server.h b/targets/include/raop_server.h
index 642fc9f..f7351c5 100644
--- a/targets/include/raop_server.h
+++ b/targets/include/raop_server.h
@@ -23,6 +23,12 @@ typedef enum { RAOP_STREAM, RAOP_PLAY, RAOP_FLUSH, RAOP_PAUSE, RAOP_STOP, RAOP_V
 typedef void (*raopsr_cb_t)(void *owner, raopsr_event_t event, ...);
 typedef void (*raop_http_cb_t)(void *owner, struct key_data_s *headers, struct key_data_s *response);
 
+typedef struct {
+	uint32_t written, read, fill;
+	uint32_t last_write_ms, last_read_ms, first_rtp_ms, first_sync_ms, first_audio_ms;
+	bool http_connected;
+} raopsr_stats_t;
+
 // set http_length to -3 for chunked-encoding, 0 for no content-length or to a positive value
 struct raopsr_s* raopsr_create(struct in_addr host, struct mdnsd *svr, char *name,
 						  char *model, unsigned char mac[6], char *stream_codec, bool stream_metadata,
@@ -33,6 +39,8 @@ struct raopsr_s* raopsr_create(struct in_addr host, struct mdnsd *svr, char *nam
 void	raopsr_update(struct raopsr_s *ctx, char *name, char *model);
 void  	raopsr_delete(struct raopsr_s *ctx);
 void	raopsr_notify(struct raopsr_s *ctx, raopsr_event_t event, void *param);
+void	raopsr_set_stream_options(struct raopsr_s *ctx, bool soft_flush, uint32_t prebuffer_ms);
+bool	raopsr_get_stats(struct raopsr_s *ctx, raopsr_stats_t *stats);
 
 void	raopsr_metadata_free(raopsr_metadata_t* data);
 void	raopsr_metadata_copy(raopsr_metadata_t* dst, raopsr_metadata_t *src);
